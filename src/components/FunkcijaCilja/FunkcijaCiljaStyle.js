import styled from 'styled-components';
import { InputSyle } from '../../generalStyles/generalStyles';

export const Container = styled.div`
`;
export const Input = styled.input`
    ${InputSyle};
    padding: 0 10px;
`;

export const Select = styled.select`
    ${InputSyle};
    padding: 5px;
`;